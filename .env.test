# Configuration Backend pour les tests
BACKEND_PORT=3002
NODE_ENV=test

# Configuration Base de Données pour les tests
DB_HOST=localhost
DB_PORT=5432
DB_NAME=gadgetzone_test
DB_USER=postgres
DB_PASSWORD=admin

# Configuration Frontend URL
FRONTEND_URL=http://localhost:5173

# Configuration JWT pour les tests
JWT_SECRET=test-jwt-secret-key-for-testing-only
JWT_EXPIRES_IN=1h

# Configuration Email (désactivée pour les tests)
EMAIL_HOST=
EMAIL_PORT=
EMAIL_USER=
EMAIL_PASSWORD=
